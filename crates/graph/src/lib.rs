//! `cow-sdk-graph` — Layer 3 `GraphQL` transport primitives for the `CoW` Protocol SDK.
//!
//! **Placeholder**: `GraphQL` request/response envelope handling, retry
//! integration with [`cow_sdk_http`], and the `GraphTransport` trait will
//! land here once extracted from [`cow_sdk_subgraph`]. Today the subgraph
//! crate uses the HTTP primitives directly.

#![deny(unsafe_code)]
#![warn(missing_docs)]
