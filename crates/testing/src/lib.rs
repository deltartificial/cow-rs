//! `cow-testing` — dev-only fixtures and helpers for the `CoW` Protocol SDK.
//!
//! This crate is **not published**. It is meant to be referenced exclusively
//! under `[dev-dependencies]` by workspace crates that want to share JSON
//! fixtures, golden files (e.g. `TypeScript` SDK parity baselines), and
//! miscellaneous mocks across their integration tests.
//!
//! **Placeholder**: the crate is currently empty. Fixtures will migrate
//! here from the individual crates' `tests/` directories incrementally.

#![deny(unsafe_code)]
#![warn(missing_docs)]
